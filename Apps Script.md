# การใช้ Apps Script สำหรับสร้างแบบสอบถาม
## สร้าง Google Form
### 1 : สั่ง AI ออกแบบฟอร์ม 

ตัวอย่าง 
Promnt 1
```
ช่วยสร้างแบบฟอร์สำหรับความพึงพอใจต่อการอบรมการวิเคราะห์ข้อมูลเบื้องต้น
```

Promnt 2
```
เขียนโค้ด Google Apps Script สร้าง Google Form และ Google Sheet
```

### 2 : สั่ง AI ออกแบบฟอร์ม 
ขั้นตอนที่ 1 
1. ไปที่เว็บไซต์ ```script.google.com```
   → แล้วคลิก โครงการใหม่ (New Project)
3. ลบโค้ดเดิมออกทั้งหมด แล้ววางโค้ด JavaScript ที่ได้ลงไป
4. กดปุ่ม บันทึก (Save)
   → และกด เรียกใช้ (Run)
5. อนุญาตสิทธิ์ (Grant Permissions) ตามขั้นตอนของ Google
เมื่อรันเสร็จ ให้ดูที่ช่อง Execution log ด้านล่าง จะได้ URL มา 2 ลิงก์:
Published Form URL:
``` ลิงก์ฟอร์มส่งให้ผู้เข้าอบรมทำแบบสอบถาม```
Google Sheet URL:
``` ลิงก์ไฟล์สเปรดชีตเก็บข้อมูลผลลัพธ์```

### 3 : วิธีดึงข้อมูลเข้า Looker Studio
เข้าไปที่ ```lookerstudio.google.com ``` คลิก สร้าง (Create)
→ แหล่งข้อมูล (Data Source) 
→ เลือกคอนเน็กเตอร์ Google ชีต (Google Sheets) 
→ เลือกไฟล์ที่สร้างขึ้นใหม่ชื่อ ```ผลประเมินความพึงพอใจ_การวิเคราะห์ข้อมูลเบื้องต้น``` เลือกแผ่นงาน ```Form Responses 1``` (หรือ การตอบแบบฟอร์ม 1)
→ ติ๊กถูกที่ตัวเลือก : 
  * ใช้แถวแรกเป็นส่วนหัว (Use first row as headers)
  * รวมเซลล์ที่ซ่อนและคัดกรองอยู่ (Include hidden and filtered cells) → กด เชื่อมต่อ (Connect) ที่มุมขวาบน


